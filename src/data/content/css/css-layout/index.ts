import type { Topic } from '../../../types'
import { flexbox } from './flexbox'
import { grid } from './grid'
import { responsive } from './responsive'

export const cssLayoutTopics: Topic[] = [
  flexbox,
  grid,
  responsive,
]
